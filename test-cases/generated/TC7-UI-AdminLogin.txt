// // Verify the Logout nav button is visible and usable on the admin login page even before any authentication, and returns the user to the homepage
open url "https://automationintesting.online/admin"
check that page contains "Logout"
click exactly "Logout"
check that page does not contain "Password"
