// Clicking Logout on the Admin Login page before authenticating simply returns to the homepage rather than indicating a successful login
check that page contains "Username"
check that page contains "Password"
click exactly "Logout"
check that page does not contain "Username"
check that page does not contain "Password"
