// // Verify that DELETE /booking/{id} with an empty token value in the Cookie header is rejected with 403
save text with parameters starting from next line and ending with [END]
{
  "firstname": "${firstname4}",
  "lastname": "${lastname4}",
  "totalprice": 175,
  "depositpaid": true,
  "bookingdates": {
    "checkin": "${checkin3}",
    "checkout": "2024-03-05"
  }
}
[END] as "bookingBody"
RR - Create booking via API using bookingBody and save booking id
call api delete from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers "Cookie:token=" and then check that http code is 403
