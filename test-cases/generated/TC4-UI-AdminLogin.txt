// Submitting the Admin Login form with only Username filled in shows the same Invalid credentials error as a full invalid submission
enter "invalid_user" into "Username"
click exactly "Login"
check that page contains "Invalid credentials"
