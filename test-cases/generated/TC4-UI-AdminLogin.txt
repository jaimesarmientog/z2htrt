// // Submitting the Admin Login form with only the username filled in shows the same Invalid credentials error
open url from string with parameters "${appBaseUrl}/admin"
enter stored value "username2" into "Username"
click "Login"
check that page contains "Invalid credentials"
