// Admin Login - entering only a username and leaving password blank shows Invalid credentials
// Admin Login - entering only a username and leaving password blank shows Invalid credentials
open url "${appBaseUrl}/admin"
enter from the string with parameters "${username}" into "Username"
click exactly "Login"
check that page contains "Invalid credentials"
