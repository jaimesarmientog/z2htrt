// // POST /auth - valid username with an invalid password returns Bad credentials
save text with parameters starting from next line and ending with [END]
{
  "username": "${username}",
  "password": "${password}"
}
[END] as "body"
RR - Submit booking request and capture rejection reason via API
check that stored value "reason" is "Bad credentials"
