// // PUT /booking/{id} - reject an update request when no auth Cookie/token header is provided (expect 403 Forbidden)
call api put from the string with parameters "${apiBaseUrl}/booking/999999" with headers "Content-Type:application/json" and body from stored value "jimBrownBreakfastBookingPayload" and then check that http code is 403
check that page contains "Forbidden"
