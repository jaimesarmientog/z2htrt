// // POST /auth - valid credentials returns a token in the response
save text starting from next line and ending with [END]
{
  "username": "admin",
  "password": "password123"
}
[END] as "body"
call api post "https://restful-booker.herokuapp.com/auth" with headers "Content-Type:application/json" and body with parameters ${body} and get "$.token" and save it as "authToken" and then check that http code is 200
