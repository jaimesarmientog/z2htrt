// POST /auth - valid username with an incorrect password returns HTTP 200 with a Bad credentials reason
save text with parameters starting from next line and ending with [END]
{
  "username": "${username}",
  "password": "${password3}"
}
[END] as "authBody"
RR - Authenticate via API and save failure reason
check that stored value "authReason" equals "Bad credentials"
