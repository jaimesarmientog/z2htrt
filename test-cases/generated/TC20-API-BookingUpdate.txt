// // Successfully authenticate, create a booking, then fully update it via PUT /booking/{id} with a valid auth token, verifying the response reflects the updated fields
RR - Authenticate as admin via API
RR - Create booking via API
save text with parameters starting from next line and ending with [END]
{
  "firstname": "${firstname2}",
  "lastname": "${lastname2}",
  "totalprice": 222,
  "depositpaid": false,
  "bookingdates": {
    "checkin": "${checkin}",
    "checkout": "2024-02-05"
  },
  "additionalneeds": "${additionalneeds}"
}
[END] as "updateBookingBody"
call api put from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers with parameters "Content-Type:application/json" and "Cookie:token=${authToken}" and body with parameters ${updateBookingBody} and get "$.firstname" and save it as "updatedFirstname" and then check that http code is 200
check that stored value "updatedFirstname" equals "James"
