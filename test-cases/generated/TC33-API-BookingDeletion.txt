// // Rejects a booking deletion request with 403 when an invalid/malformed auth token is supplied in the Cookie header, even though the booking id being targeted is real
RR - Create booking via API for Jim Brown legacy dates payload
call api delete from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers with parameters "Cookie:token=invalidToken12345" and then check that http code is 403
