// // Verify submitting invalid credentials shows the Invalid credentials error message
open url from string with parameters "${appBaseUrl}/admin"
enter "invalid_user" into "Username"
enter "invalid_pass" into "Password"
click "Login"
check that page contains "Invalid credentials"
