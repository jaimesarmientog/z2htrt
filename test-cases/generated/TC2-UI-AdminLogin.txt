// // Verify submitting the admin login form with both Username and Password left empty shows the invalid credentials error
open url from string with parameters "${appBaseUrl}/admin"
click exactly "Login"
check that page contains "Invalid credentials"
