// Reject an update request for a real, existing booking when no auth token cookie is supplied
// Reject an update request for a real, existing booking when no auth token cookie is supplied
RR - Start new JSON body block
RR - Add firstname Nina and lastname Adams to JSON body
  "totalprice": 200,
RR - Add deposit and May booking dates to JSON body
RR - Add dinner additional needs to JSON body
RR - Create booking via API and start next JSON body
RR - Add firstname Nina and lastname Adams to JSON body
  "totalprice": 250,
RR - Add deposit false and start booking dates block
    "checkin": "2024-05-10",
    "checkout": "2024-05-15"
RR - Add dinner additional needs to JSON body
}
[END] as "updateBody"
call api put from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers "Content-Type:application/json" and body with parameters ${updateBody} and get "$" and save it as "noTokenResponse" and then check that http code is 403
