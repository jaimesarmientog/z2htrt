// // Submitting the Admin Login form with only the password filled in shows the same Invalid credentials error
open url from string with parameters "${appBaseUrl}/admin"
RR - Enter password and submit login
check that page contains "Invalid credentials"
