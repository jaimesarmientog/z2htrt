// Admin Login - entering only a password and leaving username blank shows Invalid credentials
// Admin Login - entering only a password and leaving username blank shows Invalid credentials
open url "${appBaseUrl}/admin"
enter from the string with parameters "${password}" into "Password"
click exactly "Login"
check that page contains "Invalid credentials"
