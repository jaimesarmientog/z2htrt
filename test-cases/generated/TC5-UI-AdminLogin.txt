// Submitting the Admin Login form with only Password filled in shows the same Invalid credentials error as a full invalid submission
enter "invalid_pass" into "Password"
click exactly "Login"
check that page contains "Invalid credentials"
