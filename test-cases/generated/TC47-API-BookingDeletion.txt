// // Verify that DELETE /booking/{id} with an invalid (malformed) token value is rejected with 403
save text with parameters starting from next line and ending with [END]
{
  "firstname": "${firstname3}",
  "lastname": "${lastname}",
  "totalprice": 250,
  "depositpaid": false,
  "bookingdates": {
    "checkin": "${checkin2}",
    "checkout": "${checkout2}"
  },
  "additionalneeds": "${additionalneeds2}"
}
[END] as "bookingBody"
RR - Create booking via API using bookingBody and save booking id
call api delete from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers "Cookie:token=not-a-real-token" and then check that http code is 403
