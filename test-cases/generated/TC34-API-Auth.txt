// // POST /auth - empty username and empty password returns Bad credentials
save text starting from next line and ending with [END]
{
  "username": "",
  "password": ""
}
[END] as "body"
call api post "https://restful-booker.herokuapp.com/auth" with headers "Content-Type:application/json" and body with parameters ${body} and get "$.reason" and save it as "reason" and then check that http code is 200
check that stored value "reason" is "Bad credentials"
