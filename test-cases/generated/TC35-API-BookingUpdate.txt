// Successfully update an existing booking omitting the optional additionalneeds field
// Successfully update an existing booking omitting the optional additionalneeds field
RR - Start new JSON body block
RR - Add firstname Anna and lastname Smith to JSON body
  "totalprice": 150,
RR - Add deposit and March checkin date to JSON body
    "checkout": "2024-03-05"
RR - Create booking via API and authenticate via API
RR - Add firstname Anna and lastname Smith to JSON body
  "totalprice": 175,
RR - Add deposit false and March booking dates to JSON body
  }
}
[END] as "updateBodyNoNeeds"
call api put from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers with parameters "Content-Type:application/json" and "Cookie:token=${authToken}" and body with parameters ${updateBodyNoNeeds} and get "$.totalprice" and save it as "updatedTotalPrice" and then check that http code is 200
check that stored value "updatedTotalPrice" itself contains "175"
