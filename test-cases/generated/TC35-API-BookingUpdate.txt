// PUT /booking/{id} - request with an invalid (non-empty but bogus) auth token in the Cookie header is rejected with 403 per the documented 'missing or invalid auth token' response
RR - Authenticate, create booking, and prepare update booking payload via API
call api put from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers "Content-Type:application/json" and "Cookie:token=invalidtoken123" and body with parameters ${updateBookingBody} and then check that http code is 403
