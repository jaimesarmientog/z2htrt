// Reject an update request that carries an invalid auth token value
// Reject an update request that carries an invalid auth token value
RR - Start new JSON body block
RR - Add firstname Carl and lastname Jones to JSON body
  "totalprice": 90,
RR - Add deposit true and start booking dates block
    "checkin": "2024-04-01",
    "checkout": "2024-04-05"
  },
  "additionalneeds": "None"
RR - Create booking via API and start next JSON body
RR - Add firstname Carl and lastname Jones to JSON body
  "totalprice": 120,
RR - Add deposit false and start booking dates block
    "checkin": "2024-04-10",
    "checkout": "2024-04-15"
RR - Add no additional needs and close JSON body
[END] as "updateBody"
call api put from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers "Content-Type:application/json" and "Cookie:token=invalidtoken123" and body with parameters ${updateBody} and get "$" and save it as "invalidTokenResponse" and then check that http code is 403
