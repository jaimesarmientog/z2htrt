// PUT /booking/{id} - authenticated update with boundary numeric value totalprice of 0 succeeds with 200
RR - Authenticate and create booking via API
save text with parameters starting from next line and ending with [END]
{
  "firstname": "${firstname}",
  "lastname": "${lastname3}",
  "totalprice": 0,
  "depositpaid": false,
  "bookingdates": {
    "checkin": "${checkin2}",
    "checkout": "${checkout2}"
  },
  "additionalneeds": "${additionalneeds3}"
}
[END] as "updateBookingBodyZeroPrice"
call api put from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers with parameters "Content-Type:application/json" and "Cookie:token=${authToken}" and body with parameters ${updateBookingBodyZeroPrice} and get "$.totalprice" and save it as "updatedTotalprice" and then check that http code is 200
