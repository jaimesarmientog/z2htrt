// // Authentication with invalid username and password returns Bad credentials message with HTTP 200
RR - Start new JSON body block
  "username": "invalid_user",
  "password": "invalid_pass"
}
[END] as "authBody"
call api post from the string with parameters "${apiBaseUrl}/auth" with headers "Content-Type:application/json" and body with parameters ${authBody} and get "$.reason" and save it as "reason" and then check that http code is 200
check that stored value "reason" itself contains "Bad credentials"
