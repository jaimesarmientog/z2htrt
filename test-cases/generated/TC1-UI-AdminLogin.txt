// // Verify the Admin Login page loads with Username, Password fields and a Login button
open url "https://automationintesting.online/admin"
check that page contains "Username"
check that page contains "Password"
check that page contains "Login"
