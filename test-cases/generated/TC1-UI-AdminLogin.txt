// Admin Login page displays the expected form fields and actions
// Admin Login page displays the expected form fields and actions
open url "${appBaseUrl}/admin"
check that page contains "Username"
check that page contains "Password"
check that page contains exactly "Login"
check that page contains exactly "Logout"
check that page contains exactly "Toggle navigation"
