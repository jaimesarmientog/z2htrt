// // Verify that DELETE /booking/{id} successfully deletes an existing booking when a valid auth token is supplied, returning 201
save text with parameters starting from next line and ending with [END]
{
  "firstname": "${firstname2}",
  "lastname": "${lastname3}",
  "totalprice": 111,
  "depositpaid": true,
  "bookingdates": {
    "checkin": "${checkin}",
    "checkout": "${checkout}"
  },
  "additionalneeds": "${additionalneeds}"
}
[END] as "bookingBody"
RR - Create booking via API using bookingBody and save booking id
RR - Authenticate as admin via API
call api delete from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers with parameters "Cookie:token=${authToken}" and then check that http code is 201
