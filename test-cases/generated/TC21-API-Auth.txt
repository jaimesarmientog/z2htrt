// // Authenticate to /auth with a valid username but an invalid password returns 200 with a Bad credentials reason
save text starting from next line and ending with [END]
{
  "username": "admin",
  "password": "wrong_password"
}
[END] as "wrongPasswordAuthBody"
call api post from the string with parameters "${apiBaseUrl}/auth" with headers "Content-Type:application/json" and body with parameters ${wrongPasswordAuthBody} and get "$.reason" and save it as "authReason" and then check that http code is 200
check that page contains "Bad credentials"
