// Reject an update request with no auth token cookie present
// Reject an update request with no auth token cookie present
RR - Build booking JSON body with breakfast
[END] as "updateBody"
call api put from the string with parameters "${apiBaseUrl}/booking/999999" with headers "Content-Type:application/json" and body with parameters ${updateBody} and get "$" and save it as "unauthResponse" and then check that http code is 403
check that stored value "unauthResponse" itself contains "Forbidden"
