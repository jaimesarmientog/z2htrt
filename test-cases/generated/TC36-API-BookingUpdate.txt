// PUT /booking/{id} - authenticated update omitting the optional 'additionalneeds' field succeeds with 200
RR - Authenticate and create booking via API
save text with parameters starting from next line and ending with [END]
{
  "firstname": "${firstname}",
  "lastname": "${lastname3}",
  "totalprice": 222,
  "depositpaid": false,
  "bookingdates": {
    "checkin": "${checkin2}",
    "checkout": "${checkout2}"
  }
}
[END] as "updateBookingBodyNoAdditionalNeeds"
call api put from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers with parameters "Content-Type:application/json" and "Cookie:token=${authToken}" and body with parameters ${updateBookingBodyNoAdditionalNeeds} and get "$.lastname" and save it as "updatedLastname" and then check that http code is 200
