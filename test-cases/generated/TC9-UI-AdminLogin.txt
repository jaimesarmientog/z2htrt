// // Repeated invalid login attempts each consistently show the Invalid credentials error rather than locking out or changing message
RR - Attempt admin login with invalid credentials
check that page contains "Invalid credentials"
enter stored value "username2" into "Username"
RR - Enter password and submit login
check that page contains "Invalid credentials"
