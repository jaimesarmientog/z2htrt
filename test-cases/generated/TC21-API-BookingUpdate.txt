// PUT /booking/{id} - successfully updates an existing booking with a valid auth token and returns the updated booking fields with HTTP 200
RR - Authenticate via API and save token
RR - Create booking via API using standard payload
save text with parameters starting from next line and ending with [END]
{
  "firstname": "${firstname2}",
  "lastname": "${lastname2}",
  "totalprice": 222,
  "depositpaid": false,
  "bookingdates": {
    "checkin": "2024-02-01",
    "checkout": "2024-02-05"
  },
  "additionalneeds": "${additionalneeds}"
}
[END] as "updateBody"
call api put from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers with parameters "Content-Type:application/json" and "Cookie:token=${authToken}" and body with parameters ${updateBody} and get "$.firstname" and save it as "updatedFirstname" and get "$.totalprice" and save it as "updatedTotalprice" and then check that http code is 200
check that stored value "updatedFirstname" equals "James"
check that stored value "updatedTotalprice" equals "222"
