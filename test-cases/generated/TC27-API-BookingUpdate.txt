// // PUT /booking/{id} - successfully update an existing booking with a valid auth token and a full booking payload, verifying the response reflects the updated fields
RR - Authenticate as admin via API
RR - Create booking via API for Jim Brown breakfast payload
call api put from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers with parameters "Content-Type:application/json" and "Cookie:token=${authToken}" and body from stored value "jamesAllenDinnerBookingPayload" and then check that http code is 200
check that page contains "James"
check that page contains "Allen"
check that page contains "222"
check that page contains "Dinner"
