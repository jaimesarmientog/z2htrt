// DELETE /booking/{id} - request with an invalid/malformed auth token is rejected with 403
RR - Authenticate as admin via API
save text with parameters starting from next line and ending with [END]
{
  "firstname": "Anna",
  "lastname": "Smith",
  "totalprice": 250,
  "depositpaid": false,
  "bookingdates": {
    "checkin": "${checkin}",
    "checkout": "2024-02-10"
  },
  "additionalneeds": "${additionalneeds}"
}
[END] as "bookingBody"
call api post from the string with parameters "${apiBaseUrl}/booking" with headers "Content-Type:application/json" and body from stored value "bookingBody" and get "$.bookingid" and save it as "bookingId" and then check that http code is 200
call api delete from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers with parameters "Cookie:token=invalid_token_value_123" and then check that http code is 403
