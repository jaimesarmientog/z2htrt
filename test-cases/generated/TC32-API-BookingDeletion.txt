// // Rejects a booking deletion request with 403 Forbidden when no auth Cookie/token header is supplied at all
call api delete from the string with parameters "${apiBaseUrl}/booking/999999" and then check that http code is 403
