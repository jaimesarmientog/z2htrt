// DELETE /booking/{id} - request with a malformed Cookie header (missing the token key) is rejected with 403, and the booking remains deletable afterward with a valid token
RR - Authenticate as admin via API
save text starting from next line and ending with [END]
{
  "firstname": "Bob",
  "lastname": "Jones",
  "totalprice": 333,
  "depositpaid": true,
  "bookingdates": {
    "checkin": "2024-03-01",
    "checkout": "2024-03-05"
  },
  "additionalneeds": "Dinner"
}
[END] as "bookingBody"
call api post from the string with parameters "${apiBaseUrl}/booking" with headers "Content-Type:application/json" and body from stored value "bookingBody" and get "$.bookingid" and save it as "bookingId" and then check that http code is 200
call api delete from the string with parameters from string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers "Cookie:sessionId=abc123" and then check that http code is 403
call api delete from the string with parameters from string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers with parameters "Cookie:token=${authToken}" and then check that http code is 201
