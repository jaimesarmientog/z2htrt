// Authentication with a valid username but incorrect password returns 200 with a Bad credentials reason
// Authentication with a valid username but incorrect password returns 200 with a Bad credentials reason
save text with parameters starting from next line and ending with [END]
{
  "username": "admin",
  "password": "${password}"
}
[END] as "authBody"
call api post from the string with parameters "${apiBaseUrl}/auth" with headers "Content-Type:application/json" and body with parameters ${authBody} and get "$.reason" and save it as "authReason" and then check that http code is 200
check that stored value "authReason" itself contains "Bad credentials"
