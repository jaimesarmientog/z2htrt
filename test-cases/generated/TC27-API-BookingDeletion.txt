// DELETE /booking/{id} - request with no Cookie/token header on a real, existing booking id is rejected with 403
save text with parameters starting from next line and ending with [END]
{
  "firstname": "Alice",
  "lastname": "${lastname2}",
  "totalprice": 250,
  "depositpaid": false,
  "bookingdates": {
    "checkin": "2024-03-01",
    "checkout": "2024-03-10"
  },
  "additionalneeds": "${additionalneeds}"
}
[END] as "bookingBody"
RR - Create booking via API using custom payload
call api delete from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" and then check that http code is 403
