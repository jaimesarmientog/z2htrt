// // Verify POST /auth treats a request with a correct username but missing password field the same as invalid credentials
save text starting from next line and ending with [END]
{
  "username": "admin"
}
[END] as "authBody"
call api post "https://restful-booker.herokuapp.com/auth" with headers "Content-Type:application/json" and body with parameters ${authBody} and get "$.reason" and save it as "reasonValue" and then check that http code is 200
check that stored value "reasonValue" equals "Bad credentials"
