// Authentication with invalid credentials returns 200 with a Bad credentials reason
// Authentication with invalid credentials returns 200 with a Bad credentials reason
save text starting from next line and ending with [END]
{
  "username": "invalid_user",
  "password": "invalid_pass"
}
[END] as "authBody"
call api post "https://restful-booker.herokuapp.com/auth" with headers "Content-Type:application/json" and body with parameters ${authBody} and get "$.reason" and save it as "authReason" and then check that http code is 200
check that stored value "authReason" itself contains "Bad credentials"
