// PUT /booking/{id} - request with no Cookie/token header is rejected with 403 Forbidden
save text with parameters starting from next line and ending with [END]
{
  "firstname": "${firstname2}",
  "lastname": "${lastname2}",
  "totalprice": 111,
  "depositpaid": true,
  "bookingdates": {
    "checkin": "${checkin}",
    "checkout": "${checkout}"
  },
  "additionalneeds": "${additionalneeds}"
}
[END] as "bookingBody"
call api put from the string with parameters "${apiBaseUrl}/booking/999999" with headers "Content-Type:application/json" and body with parameters ${bookingBody} and then check that http code is 403
