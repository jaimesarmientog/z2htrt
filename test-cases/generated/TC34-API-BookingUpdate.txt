// Successfully update all fields of an existing booking using a valid auth token
// Successfully update all fields of an existing booking using a valid auth token
save text starting from next line and ending with [END]
{
  "firstname": "James",
  "lastname": "Brown",
  "totalprice": 111,
  "depositpaid": true,
  "bookingdates": {
    "checkin": "2024-01-01",
    "checkout": "2024-01-05"
  },
  "additionalneeds": "Breakfast"
}
[END] as "createBody"
call api post "https://restful-booker.herokuapp.com/booking" with headers "Content-Type:application/json" and body with parameters ${createBody} and get "$.bookingid" and save it as "bookingId" and then check that http code is 200
save text starting from next line and ending with [END]
{
  "username": "admin",
  "password": "password123"
}
[END] as "authBody"
call api post "https://restful-booker.herokuapp.com/auth" with headers "Content-Type:application/json" and body with parameters ${authBody} and get "$.token" and save it as "authToken" and then check that http code is 200
save text starting from next line and ending with [END]
{
  "firstname": "UpdatedJames",
  "lastname": "UpdatedBrown",
  "totalprice": 222,
  "depositpaid": false,
  "bookingdates": {
    "checkin": "2024-02-01",
    "checkout": "2024-02-05"
  },
  "additionalneeds": "Lunch"
}
[END] as "updateBody"
call api put from the string with parameters "https://restful-booker.herokuapp.com/booking/${bookingId}" with headers with parameters "Content-Type:application/json" and "Cookie:token=${authToken}" and body with parameters ${updateBody} and get "$.firstname" and save it as "updatedFirstname" and get "$.lastname" and save it as "updatedLastname" and get "$.totalprice" and save it as "updatedTotalPrice" and get "$.depositpaid" and save it as "updatedDepositPaid" and get "$.additionalneeds" and save it as "updatedAdditionalNeeds" and then check that http code is 200
check that stored value "updatedFirstname" itself contains "UpdatedJames"
check that stored value "updatedLastname" itself contains "UpdatedBrown"
check that stored value "updatedTotalPrice" itself contains "222"
check that stored value "updatedDepositPaid" itself contains "false"
check that stored value "updatedAdditionalNeeds" itself contains "Lunch"
