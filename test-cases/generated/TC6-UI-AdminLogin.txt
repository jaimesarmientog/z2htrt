// Admin login - special characters in credentials still shows Invalid credentials, not a server error
open url "${appBaseUrl}/admin"
enter "' OR 1=1 --" into "Username"
enter "<script>alert(1)</script>" into "Password"
RR - Submit admin login and verify invalid credentials error
