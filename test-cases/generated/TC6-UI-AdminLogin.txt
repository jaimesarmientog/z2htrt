// Admin Login - repeated invalid submissions consistently return the same Invalid credentials message
// Admin Login - repeated invalid submissions consistently return the same Invalid credentials message
RR - Open admin login page and click login
check that page contains "Invalid credentials"
RR - Enter invalid login credentials and click login
check that page contains "Invalid credentials"
