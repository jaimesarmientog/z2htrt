// // PUT /booking/{id} - reject an update request when an invalid auth token value is supplied in the Cookie header (expect 403)
RR - Authenticate as admin via API
RR - Create booking via API for Jim Brown breakfast payload
call api put from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers "Content-Type:application/json" and "Cookie:token=invalidTokenValue123" and body from stored value "jamesAllenDinnerBookingPayload" and then check that http code is 403
