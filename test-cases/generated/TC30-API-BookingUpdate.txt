// Updating a booking with an invalid auth token returns 403 Forbidden
// Updating a booking with an invalid auth token returns 403 Forbidden
RR - Authenticate via API and create booking via API
save text with parameters starting from next line and ending with [END]
{
  "firstname": "${firstname}",
  "lastname": "${lastname2}",
  "totalprice": 200,
  "depositpaid": false,
  "bookingdates": {
    "checkin": "${checkin}",
    "checkout": "${checkout}"
  }
}
[END] as "updateBody"
call api put from the string with parameters from the string with parameters "${apiBaseUrl}/booking/${bookingId}" with headers with parameters "Content-Type:application/json" and "Cookie:token=invalid-bogus-token-value" and body with parameters ${updateBody} and then check that http code is 403
