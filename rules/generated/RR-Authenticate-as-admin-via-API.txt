// Sends a POST request to the auth endpoint with stored admin login credentials, extracts the returned token, saves it as authToken, and verifies a 200 HTTP response
call api post from the string with parameters "${apiBaseUrl}/auth" with headers "Content-Type:application/json" and body from stored value "adminLoginCredentials" and get "$.token" and save it as "authToken" and then check that http code is 200
